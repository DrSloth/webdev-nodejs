// Newer versions of JavaScript support a very handy string formatting syntax using ``

let x = 10;
// We can use ${} to directly embed any expression
let s = `${x} times two is ${x*2}`;

console.log(s);
